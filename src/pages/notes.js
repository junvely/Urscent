import React from "react";
import Header from "../components/header";

const Notes = (props) => {
  return (
    <>
      <Header></Header>
      <div>notes 입니다.</div>
    </>
  );
};

export default Notes;
